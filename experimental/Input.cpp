#include "Input.h"

/*template<typename T>
Action::Action(void(*func)(T))
{
	setFunc(func);
}

template<typename T>
void Action::setFunc(void(*func)(T))
{
	this->action = func;
}

template<typename T>
void Action::exec(T param)
{
	(*action)(param);
}

template<typename T>
void Event::addHandler(Action<T> handler)
{
	handlers.push_back(handler);
}

template<typename T>
void Event::exec(T param)
{
	for (int i = 0; i < handlers.length; i++)
	{
		handlers[i].exec(param);
	}
}*/
